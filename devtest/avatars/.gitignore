# Local avatar overrides are personal, not fixtures — see ui.LoadAvatarDir.
# The generated ones below are checked in; anything else you drop here is
# yours.
*
!.gitignore
!gen.go
!alice@localhost.png
!bob@localhost.png
!carol@localhost.png
